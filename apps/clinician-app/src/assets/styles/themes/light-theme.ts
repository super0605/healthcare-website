import { Theme } from '@medopad/shared/theme';

export default {
  name: 'clinician-light-theme',
  variables: {
    // '@body-background': '#F4F5F5',
    // '@primary-color': '#ba1be9',
  }
} as Theme;
